const express = require('express')
const cookieParser = require('cookie-parser')


const app = express();
app

ap.use(express.jaon());
app.use(cookieParser());














module.exports = app;

// 

// 